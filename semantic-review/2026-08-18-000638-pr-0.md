# Retry-safe WhatsApp OTP login with independently verified callback evidence

This final regression pass finds no remaining repository defect in the WhatsApp login hardening. The delayed-cookie, stale-session replay, executable completion coverage, and self-attested callback-readiness blockers are resolved. Ed25519 verification makes the gate a verifier only: repository production runtime has no signer, private-key input, or evidence-construction helper. Repository validation is complete, while production callback readiness remains closed until an independent deployed collector supplies fresh authenticated evidence.

Watch for: **confirmed** local transaction-capable MongoDB is unavailable, so required-fixture validation intentionally fails its three Mongo cases; **confirmed** no authenticated deployed callback evidence was supplied, so this review does not claim external callback readiness.

**Verdict**: APPROVED

## High-level view

Cookie attachment uses each JWT's immutable expiration relative to response time, and completion replay verifies account status, token version, and the persisted refresh hash. Delayed retries cannot extend cookie validity or restore a session invalidated by refresh, logout, blocking, or a version change.

The callback gate verifies canonical evidence with a configured Ed25519 SPKI public key. The signature binds producer, environment, deployment, callback URL, UUID run, allow-list confirmation, five constrained checks, and timestamps; freshness and an atomic replay marker limit the artifact to one ten-minute-bound run.

Property seeds, focused integrations, the full suite, lint, diff check, and exact dependency check pass. Ordinary validation skips only three unavailable Mongo transaction cases, while required-fixture mode converts those cases into failures with zero skips.

<details>
<summary>Issues (0)</summary>

No repository defects remain from the reviewed change.

</details>

<details><summary>Details</summary>

## The four original blockers are closed

**Confirmed.** `tokenLifetimeMs` derives remaining validity from absolute `exp`; controlled-clock coverage confirms delayed replay cookies end with their JWTs. Completion replay reconstructs deterministic tokens only after matching active status, token version, and the persisted refresh hash with timing-safe comparison. Runtime and model coverage reject refresh rotation, logout, blocking, and version changes, then terminally close stale proofs.

**Confirmed.** The completion harness executes lookup, creation, update, token creation, refresh persistence, completion insertion, ambiguous acknowledgement, pre-commit failures, blocked denial, and 2–20 concurrent retries. The three real-Mongo cases add transaction integration and remain mandatory under required-fixture policy.

**Confirmed.** `scripts/verify-whatsapp-callback-readiness.js` imports an Ed25519 public key from base64 DER and uses `crypto.verify` over canonical unsigned evidence. It has no private-key setting, signing export, `crypto.sign`, key generation, shared HMAC secret, or helper capable of manufacturing passing evidence. Private-key generation and signing exist only in the readiness test fixture to create synthetic verifier inputs.

## Independent evidence remains a deployment prerequisite

**Confirmed.** Evidence must validate under the configured producer key and match producer, environment, deployment, exact callback URL, run ID, validity window, and check timestamps. The signed facts are constrained to Meta reachability, GET challenge success, `messages` subscription, exact-body signed request correlation, and invalid-signature HTTP 401 without state mutation; the run marker is claimed atomically.

Trust still depends on deployment controls pinning the collector public key and keeping its private key unavailable to the gate and operator process. No deployed artifact was supplied. The CLI without evidence exited 1 with all five failed-check names, and `--phone SYNTHETIC_TARGET` was rejected with exit 1 without echoing the argument; production readiness remains closed and no external Meta action is claimed.

## Validation separates repository behavior from unavailable MongoDB

**Confirmed.** The focused command was:

```text
node --import ./tests/setup-env.js --test --test-concurrency=1 tests/whatsapp-login-bug-condition.property.test.js tests/whatsapp-login-preservation.property.test.js tests/auth-credential-validation.test.js tests/env-config.test.js tests/api.integration.test.js tests/login-completion.test.js tests/otp-security.test.js tests/otp-diagnostics.test.js tests/otp-notification-worker.test.js tests/whatsapp-provider.test.js tests/token-cookie.test.js tests/whatsapp-callback-readiness.test.js
```

It ran 99 tests: 96 passed, 0 failed, and 3 Mongo transaction tests skipped on `ECONNREFUSED 127.0.0.1:27017`. Seeds `20260607` and `20260608`, Redis proof/correlation, BullMQ, Express, webhook, cookie, documentation, readiness, startup, and completion-model coverage executed.

`npm test` ran 168 tests: 165 passed, 0 failed, and the same 3 skips. `npm run test:required-fixtures` ran 168 tests: 165 passed, 3 expected Mongo-unavailable failures, and 0 skipped. `npm run lint`, `git diff --check HEAD`, and `npm ls fast-check --depth=0` passed; `fast-check@3.23.2` is exact in the manifest, lockfile, and installed tree.

`npm run verify:whatsapp-callback-readiness` and `npm run verify:whatsapp-callback-readiness -- --phone SYNTHETIC_TARGET` both exited 1 with sanitized output. Tests preload `tests/setup-env.js` with `NODE_ENV=test` and synthetic settings before imports, so `src/config/env.js` bypasses `dotenv.config()`. The local `.env` was not read or modified.

## Startup and worktree stay fail-safe and scoped

**Confirmed.** Environment parsing accepts only exact booleans and implemented providers, validates Meta fields whenever Meta is selected, and reports value-free reasons. Production connection provisions completion uniqueness and rejects MongoDB without transaction support; production health returns 503 when that check fails.

`HEAD` and `main` both resolve to `74a74ab9991a79da20ab041af10626db4f652931`. The local delta contains only the intended environment example, WhatsApp documentation, lint/package/lock changes, auth/config source, readiness script, tests/helpers, and semantic reports; validation created no extra artifact.

</details>

<details>
<summary>File map</summary>

- `.env.example` — documents callback evidence, replay, deployment binding, and fixture policy.
- `docs/whatsapp-login-otp.md` — documents the timing-only API contract and independent evidence workflow.
- `eslint.config.js` — adds readiness scripts to lint coverage.
- `package.json`, `package-lock.json` — add deterministic validation commands and exact `fast-check@3.23.2` locking.
- `src/app.js`, `src/config/database.js`, `src/config/env.js` — enforce transaction-aware health/startup and strict sanitized configuration.
- `src/modules/auth/auth.routes.js`, `src/modules/auth/auth.service.js`, `src/modules/auth/token.service.js` — align cookies with token expiry and make completion transactional, deterministic, and stale-aware.
- `src/modules/auth/auth.validation.js`, `src/modules/auth/otp.constants.js`, `src/modules/auth/phone.js` — enforce exact phone and OTP grammars.
- `src/modules/auth/login-completion.model.js` — stores immutable proof-digest completion metadata.
- `src/modules/auth/otp-diagnostics.js`, `src/modules/auth/otp-notification.worker.js`, `src/modules/auth/secure-otp.service.js`, `src/modules/auth/whatsapp.webhook.js` — add bounded proofs, sanitized correlation, safe worker outcomes, and signature-first webhook processing.
- `scripts/verify-whatsapp-callback-readiness.js` — verifies fresh, bound, single-use Ed25519 evidence without signing capability.
- `tests/admin-production.test.js`, `tests/api.integration.test.js`, `tests/catalog-stock.test.js`, `tests/helpers/http.js` — adapt existing application and production checks.
- `tests/auth-credential-validation.test.js`, `tests/env-config.test.js` — cover exact credentials and fail-safe startup.
- `tests/helpers/login-completion-harness.js`, `tests/integration-fixture-policy.js`, `tests/login-completion.test.js` — execute completion failures, concurrency, stale replay, transactions, and fixture enforcement.
- `tests/otp-diagnostics.test.js`, `tests/otp-notification-worker.test.js`, `tests/otp-security.test.js` — cover diagnostics, BullMQ outcomes, Redis proofs, and correlation.
- `tests/setup-env.js`, `tests/setup-required-fixtures.js` — provide synthetic test configuration and fail-closed fixture mode.
- `tests/token-cookie.test.js`, `tests/whatsapp-callback-readiness.test.js` — cover delayed expiry and authenticated readiness behavior.
- `tests/whatsapp-login-bug-condition.property.test.js`, `tests/whatsapp-login-preservation.property.test.js` — replay correction and preservation seeds.
- `tests/whatsapp-provider.test.js` — covers provider and exact-body webhook behavior.
- `semantic-review/2026-08-17-204323-pr-0.md`, `semantic-review/2026-08-17-231944-pr-0.md`, `semantic-review/2026-08-17-235307-pr-0.md`, `semantic-review/2026-08-18-000638-pr-0.md` — preserve review passes.

Full tracked diff: `git diff HEAD`. Include untracked additions with `git ls-files --others --exclude-standard`.

</details>
